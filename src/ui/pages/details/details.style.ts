import { StyleSheet } from "react-native";
const detailStyle = StyleSheet.create({
    container: {
      flex: 1,
      padding: 16,
    },
    title: {
      fontSize: 24,
      fontWeight: 'bold',
    },
  });

  export default detailStyle;